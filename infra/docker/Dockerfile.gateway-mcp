FROM node:20-alpine AS base
RUN corepack enable && corepack prepare pnpm@9.15.4 --activate
WORKDIR /app

FROM base AS deps
COPY pnpm-lock.yaml pnpm-workspace.yaml package.json ./
COPY apps/gateway-mcp/package.json ./apps/gateway-mcp/
COPY packages/core/package.json ./packages/core/
COPY packages/config/package.json ./packages/config/
COPY packages/db/package.json ./packages/db/
COPY packages/connectors/package.json ./packages/connectors/
RUN pnpm install --frozen-lockfile

FROM base AS builder
COPY --from=deps /app/node_modules ./node_modules
COPY --from=deps /app/apps/gateway-mcp/node_modules ./apps/gateway-mcp/node_modules
COPY . .
RUN pnpm build --filter=@sovereign/gateway-mcp...

FROM base AS runner
ENV NODE_ENV=production
COPY --from=builder /app/apps/gateway-mcp/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
EXPOSE 3003
HEALTHCHECK --interval=30s --timeout=10s --start-period=10s --retries=3 \
  CMD node -e "process.exit(0)"
CMD ["node", "dist/index.js"]
